import SwiftUI

struct Player: View {
    
    var body: some View {
        
        Circle()
            .foregroundColor(.green)
            .frame(width: 35, height: 35)
            
    }
}

#Preview {
    Player()
}
